php-fpm 属于预派生子进程模式
预派生子进程模式
	程序启动后就会创建N个进程，每个子进程进入Accept，等待新的连接进入。
	当客户端连接到服务器时，其中一个子进程会被唤醒，开始处理客户端请求，
	并且不再接受新的TCP连接。当此连接关闭时，子进程会释放，
	重新进入Accept,参与处理新的连接
	
优势：完全可以复用进程，不需要太多的上下文切换

劣势:
	一种依赖进程的数量解决并发问题，一个客户端连接就需要占用一个进程，
	工作进程的数量有多少，并发处理能力就有多少。
	启用大量进程会带来额外的进程调度消耗
	
解决方案:
	使用IO复用技术